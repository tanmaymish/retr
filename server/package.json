{
  "name": "@akshayvridhi/server",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Akshayvridhi API — auth, household twin storage, and the financial decision engine.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test --test-concurrency=1 \"test/*.test.js\"",
    "seed": "node scripts/seed.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "better-sqlite3": "^12.6.0",
    "cookie-parser": "^1.4.7",
    "express": "^5.2.0",
    "multer": "^2.0.2",
    "zod": "^4.1.14"
  }
}
